services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./poll_data.db:/app/poll_data.db
    environment:
      # Match your local timezone so POLL_HOUR/REPORT_HOUR are correct
      TZ: America/Los_Angeles
